#include "raylib.h"

//int ChecarRet(Rectangle mario, Rectangle cr7){
    
    /* if(CheckCollisionRecs(mario, cr7)==1){
         
        if( mario.y + mario.height > cr7.y && mario.y < cr7.y + cr7.height && mario.x < cr7.x) return 1;
            
        else if( mario.x + mario.width > cr7.x && mario.x < cr7.x + cr7.width && mario.y < cr7.y) return 2;
        
        else if( mario.y + mario.height > cr7.y && mario.y < cr7.y + cr7.height && mario.x + mario.width > cr7.x + cr7.width) return 3;
        
        else if( mario.x + mario.width > cr7.x + mario.width && mario.x  < cr7.x + cr7.width + mario.width && mario.y + mario.height > cr7.y + cr7.height) return 4;

        else return 0;
            
     }
     
     else
         return 0;
}*/

int main(void)
{
    // Initialization
    //--------------------------------------------------------------------------------------
    const int screenWidth = 1760;       //define a largura da tela
    const int screenHeight = 1000;      //define a altura da tela
    int posicaoAuxX1 = 0, posicaoAuxX2 = 0, posicaoAuxY1 = 0, posicaoAuxY2 = 0, flag_d=0, flag_e=0, flag_c=0, flag_b=0, recuo=11;
    char flag;

    InitWindow(screenWidth, screenHeight, "raylib [core] example - keyboard input");
    
    Texture2D texture  = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\mapa.png");        // Carrega as texturas a partir de imagens da galeria
    Texture2D texture2 = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\mario_direita.png");
    Texture2D texture3 = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\mario_esquerda.png");
    Texture2D texture4 = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\mario_parado.png");
    Texture2D texture5 = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\mario_cima.png");
    Texture2D texture6 = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\mario_baixo.png");
    Texture2D texture7 = LoadTexture("C:\\Users\\CIn\\Downloads\\mario_img\\cr7.png");

    Vector2 personagem = { 200, 200 };   
    //inicia o personagem no centro da tela
    Rectangle cr7 = {500, 500, 300, 300};
    Rectangle mario = {personagem.x, personagem.y, 48, 75};

    SetShapesTexture(texture7, cr7);

    SetTargetFPS(60);               // Set our game to run at 60 frames-per-second
    //--------------------------------------------------------------------------------------

    while (!WindowShouldClose()){   // Detect window close button or ESC key

        // Update
        posicaoAuxX1=personagem.x;        //variaveis aux que eu uso pra mudar a imagem do pers de acordo com a posicao q ele ta indo
        posicaoAuxY1=personagem.y;

        //borda lateral = screenWidth/2 - texture.width/2
        //tamanho ate a borda = (screenWidth/2 - texture.width/2) + screen.width/2
        
    mario.x = personagem.x;
    mario.y = personagem.y;

    
    
    
    
        if (personagem.x + texture2.width < (screenWidth/2 + texture.width/2))        //so move pra direita se a posicao x do pers for menor q
            if (IsKeyDown(KEY_RIGHT)||IsGamepadButtonDown(0, GAMEPAD_BUTTON_LEFT_FACE_RIGHT)){
                personagem.x += 4.0f;   
                flag = 'd';
                flag_d=1;
            }
        
        if (personagem.x > (screenWidth - texture.width)/2)
            if (IsKeyDown (KEY_LEFT)|| IsGamepadButtonDown(0, GAMEPAD_BUTTON_LEFT_FACE_LEFT)){ 
                personagem.x -= 4.0f;
                flag = 'e';
                flag_e=1;
            }
        
        if(personagem.y > (screenHeight - texture.height)/2)                          //so move pra cima se a posicao y do pers for menor q
            if (IsKeyDown(KEY_UP) || IsGamepadButtonDown(0, GAMEPAD_BUTTON_LEFT_FACE_UP)){ 
                personagem.y -= 4.0f; 
                flag = 'c';
                flag_c=1;
            }                
        
        if (personagem.y + texture2.height < (screenHeight/2 + texture.height/2))
            if (IsKeyDown(KEY_DOWN)||IsGamepadButtonDown(0, GAMEPAD_BUTTON_LEFT_FACE_DOWN)) {
                personagem.y += 4.0;
                flag = 'b';
                flag_b=1;
            }
   
    
    if(CheckCollisionRecs(mario, cr7)){                   // se existir colisao ele entra
        
            if (flag == 'e') {
                if(flag_b)                                //
                    personagem.y-=recuo;                  //dentro destes if's, dependendo qual botao ele aperta, ele dara uma velocidade inversa
                if(flag_c)                                //
                    personagem.y += recuo;
                if(flag_d)
                    personagem.x -= recuo;
                personagem.x += recuo;
            }
                              
            if (flag == 'c') {
                if(flag_e)
                    personagem.x += recuo;
                if(flag_d)
                    personagem.x -= recuo;
                if(flag_b)
                    personagem.y -= recuo;
                personagem.y += recuo;
            }
        
            if (flag == 'b') {
                if(flag_e)
                    personagem.x += recuo;
                if(flag_d)
                    personagem.x -= recuo;
                if(flag_c)
                    personagem.y += recuo;
                personagem.y -= recuo;
            }
             
            if (flag == 'd') {
                if(flag_b)
                    personagem.y-=recuo;
                if(flag_c)
                    personagem.y += recuo;
                if(flag_e)
                    personagem.x += recuo;
                personagem.x -= recuo;
            }
    }
    flag_b=0;
    flag_c=0;
    flag_d=0;
    flag_e=0;
    
    
        posicaoAuxX2=personagem.x;
        posicaoAuxY2=personagem.y;
        


//------------------------------------------------------------------------------------------------- 
        BeginDrawing();
        
        RLAPI void HideCursor(void);                                      // Hides cursor


            ClearBackground(RAYWHITE);
            
            DrawTexture(texture, screenWidth/2 - texture.width/2, screenHeight/2 - texture.height/2, WHITE); //desenha o mapa no centro da tela
            
            DrawTexture(texture7, 500, 500, WHITE);
            
            //DrawRectangle(screenWidth/2 - texture7.width/2, screenHeight/2 - texture7.height/2, 300, 300, RAYWHITE);
            

            if(posicaoAuxX1 < posicaoAuxX2)                     //usa as variaveis auxiliares para saber se houve algm movimento,
            DrawTextureV(texture2 , personagem, WHITE);        //dependendo de como de moveu, a imagem do mario q aparece muda
            
            else if(posicaoAuxX1 > posicaoAuxX2)
            DrawTextureV(texture3, personagem, WHITE);
        
            else if(posicaoAuxY1 > posicaoAuxY2)
            DrawTextureV(texture5, personagem, WHITE);
        
            else if(posicaoAuxY1 < posicaoAuxY2)
            DrawTextureV(texture6, personagem, WHITE);

            else if(posicaoAuxY1 == posicaoAuxY2 || posicaoAuxX1 == posicaoAuxX2)
            DrawTextureV(texture4, personagem, WHITE);
       
        
            //DrawCircleV(personagem, 50, MAROON);

        EndDrawing();
        //----------------------------------------------------------------------------------
    }

    // De-Initialization
    //--------------------------------------------------------------------------------------
    CloseWindow();        // Close window and OpenGL context
    //--------------------------------------------------------------------------------------

    return 0;
}
