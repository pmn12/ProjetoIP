#include "raylib.h"


int main(void)
{
    // Initialization
    //--------------------------------------------------------------------------------------
    const int screenWidth = 1760;       //define a largura da tela
    const int screenHeight = 1000;      //define a altura da tela
    int posicaoAuxX1 = 0, posicaoAuxX2 = 0, posicaoAuxY1 = 0, posicaoAuxY2 = 0;

    InitWindow(screenWidth, screenHeight, "raylib [core] example - keyboard input");
    
    Texture2D texture = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\mapa.png");        // Carrega as texturas a partir de imagens da galeria
    Texture2D texture2 = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\mario_direita.png");
    Texture2D texture3 = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\mario_esquerda.png");
    Texture2D texture4 = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\mario_parado.png");
    Texture2D texture5 = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\mario_cima.png");
    Texture2D texture6 = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\mario_baixo.png");
    Texture2D texture7 = LoadTexture("C:\\Users\\paulo\\Downloads\\mario_img\\cr7.png");

    Vector2 ballPosition = { (float)screenWidth/2, (float)screenHeight/2 };         //inicia o personagem no centro da tela

    SetTargetFPS(60);               // Set our game to run at 60 frames-per-second
    //--------------------------------------------------------------------------------------

    while (!WindowShouldClose()){   // Detect window close button or ESC key

        // Update
        posicaoAuxX1=ballPosition.x;        //variaveis aux que eu uso pra mudar a imagem do pers de acordo com a posicao q ele ta indo
        posicaoAuxY1=ballPosition.y;

        //borda lateral = screenWidth/2 - texture.width/2
        //tamanho ate a borda = (screenWidth/2 - texture.width/2) + screen.width/2
        
       
        
        if (ballPosition.x + texture2.width < (screenWidth/2 + texture.width/2))        //so move pra direita se a posicao x do pers for menor q
            if (IsKeyDown(KEY_RIGHT)) ballPosition.x += 3.0f;                           //a borda do mapa
        
        if (ballPosition.x > (screenWidth - texture.width)/2)
            if (IsKeyDown (KEY_LEFT)) ballPosition.x -= 3.0f;
        
        if(ballPosition.y > (screenHeight - texture.height)/2)                          //so move pra cima se a posicao y do pers for menor q
            if (IsKeyDown(KEY_UP)) ballPosition.y -= 3.0f;                              //a borda dp mapa
        
        if (ballPosition.y + texture2.height < (screenHeight/2 + texture.height/2))
            if (IsKeyDown(KEY_DOWN)) ballPosition.y += 3.0f;
        
        posicaoAuxX2=ballPosition.x;
        posicaoAuxY2=ballPosition.y;
        


//------------------------------------------------------------------------------------------------- 
        BeginDrawing();
        
        RLAPI void HideCursor(void);                                      // Hides cursor


            ClearBackground(RAYWHITE);
            
            DrawTexture(texture, screenWidth/2 - texture.width/2, screenHeight/2 - texture.height/2, WHITE); //desenha o mapa no centro da tela
            
            DrawTexture(texture7, 200, 200, WHITE);

            
            

            if(posicaoAuxX1 < posicaoAuxX2)                     //usa as variaveis auxiliares para saber se houve algm movimento,
            DrawTextureV(texture2, ballPosition, WHITE);        //dependendo de como de moveu, a imagem do mario q aparece muda
            
            else if(posicaoAuxX1 > posicaoAuxX2)
            DrawTextureV(texture3, ballPosition, WHITE);
        
            else if(posicaoAuxY1 > posicaoAuxY2)
            DrawTextureV(texture5, ballPosition, WHITE);
        
            else if(posicaoAuxY1 < posicaoAuxY2)
            DrawTextureV(texture6, ballPosition, WHITE);

            else if(posicaoAuxY1 == posicaoAuxY2 || posicaoAuxX1 == posicaoAuxX2)
            DrawTextureV(texture4, ballPosition, WHITE);
        
            //DrawCircleV(ballPosition, 50, MAROON);

        EndDrawing();
        //----------------------------------------------------------------------------------
    }

    // De-Initialization
    //--------------------------------------------------------------------------------------
    CloseWindow();        // Close window and OpenGL context
    //--------------------------------------------------------------------------------------

    return 0;
}
